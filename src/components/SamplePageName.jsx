import React from "react";

const SamplePageName = ({ text }) => {
  return (
    <div className="text-6xl flex items-center justify-center h-[90vh]">
      {text}
    </div>
  );
};

export default SamplePageName;
